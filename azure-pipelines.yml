# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main
- tonychue-patch-1

pool:
  vmImage: ubuntu-latest

stages:
# - stage: Plan
#   displayName: 'Terraform Plan'
#   jobs:
#   - job: TerraformPlan
#     displayName: 'Terraform Plan'
#     steps:
#     - task: TerraformInstaller@0
#       inputs:
#         terraformVersion: 'latest'
#     - task: TerraformTask@5
#       inputs:
#         provider: 'azurerm'
#         command: 'init'
#         backendServiceArm: 'HCP Terraform (contoso-ltd)'
#         backendAzureRmResourceGroupName: 'rg_devops'
#         backendAzureRmStorageAccountName: 'tfdevopsaccount'
#         backendAzureRmContainerName: 'tf-dev-state-container'
#         backendAzureRmKey: 'uksouth/terraform.state'


#     - script: |
#         terraform --version
#         terraform init -backend-config="storage_account_name=$(storageAccountName)" -backend-config="container_name=$(containerName)" -backend-config="key=$(key)" -backend-config="access_key=$(accessKey)"
#         terraform plan -out=tfplan
#       displayName: 'Terraform Init and Plan'

# - stage: Deploy
#   displayName: 'Terraform Apply'
#   jobs:
#   - job: TerraformApply
#     displayName: 'Terraform Apply'
#     steps:
#     - task: TerraformInstaller@0
#       inputs:
#         terraformVersion: 'latest'

#     - script: |
#         terraform --version
#         terraform init -backend-config="storage_account_name=$(storageAccountName)" -backend-config="container_name=$(containerName)" -backend-config="key=$(key)" -backend-config="access_key=$(accessKey)"
#         terraform apply -auto-approve tfplan
#       displayName: 'Terraform Init and Apply'



- stage: Plan
  displayName: 'Terraform Plan'

  jobs:
  - job: TerraformPlan

    steps:

    - checkout: self

    - task: TerraformInstaller@0
      displayName: 'Install Terraform'
      inputs:
        terraformVersion: 'latest'

    - task: AzureCLI@2
      displayName: 'Azure Login and Terraform Init'
      inputs:
        azureSubscription: 'testsvc'
        scriptType: 'bash'
        scriptLocation: 'inlineScript'
        addSpnToEnvironment: true
        inlineScript: |
          set -e

          echo "Azure account:"
          az account show

          echo "Subscription:"
          az account show --query id -o tsv
          az storage blob list \
            --account-name tfdevopsaccount \
            --container-name tf-dev-state-container \
            --auth-mode login \
            --output table
          terraform init \
            -backend-config="storage_account_name=tfdevopsaccount" \
            -backend-config="container_name=tf-dev-state-container" \
            -backend-config="key=uksouth/terraform.state" \
            -backend-config="resource_group_name=rg_devops" \
            -backend-config="subscription_id=732ec2db-5140-4675-bf72-0dd352a999a8" \
            -backend-config="use_azuread_auth=true"

    - task: TerraformTask@5
      displayName: 'Terraform Plan'
      inputs:
        provider: 'azurerm'
        command: 'plan'
        environmentServiceNameAzureRM: 'HCP Terraform (contoso-ltd)'